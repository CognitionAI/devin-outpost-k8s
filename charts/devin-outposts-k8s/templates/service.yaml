{{- if .Values.metrics.service.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "devin-outposts-k8s.fullname" . }}-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "devin-outposts-k8s.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "devin-outposts-k8s.selectorLabels" . | nindent 4 }}
  ports:
    - name: metrics
      port: {{ .Values.metrics.service.port }}
      targetPort: metrics
{{- end }}
