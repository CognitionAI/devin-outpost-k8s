outposts-operator {{ .Chart.AppVersion }} installed as {{ include "outposts-operator.fullname" . }}.

NOTE: This is a scaffold. The operator starts, serves metrics, and watches
OutpostPool resources, but the reconcile/claim/worker logic is not implemented
yet (see docs/ARCHITECTURE.md).

{{- if .Values.defaultPool.enabled }}

A default OutpostPool "{{ include "outposts-operator.fullname" . }}-default" was created for pool {{ .Values.defaultPool.poolId | quote }}.
{{- else }}

No OutpostPool was created. Create one to start serving a pool, e.g.:

  kubectl apply -f https://raw.githubusercontent.com/usacognition/outposts-kubernetes/main/examples/outpostpool.yaml

Or re-install with --set defaultPool.enabled=true --set defaultPool.poolId=<id> ...
{{- end }}

Metrics: {{ if .Values.metrics.service.enabled }}http://{{ include "outposts-operator.fullname" . }}-metrics.{{ .Release.Namespace }}:{{ .Values.metrics.service.port }}/metrics{{ else }}disabled{{ end }}
